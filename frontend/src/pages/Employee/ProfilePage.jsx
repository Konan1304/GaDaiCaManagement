import { useOutletContext } from "react-router-dom";
import { FiLogOut, FiMail, FiMapPin, FiPhone, FiUser } from "react-icons/fi";
import { shortDate } from "../../utils/employeeFormat";
export default function ProfilePage(){const {profile,loading,logout}=useOutletContext();if(loading)return <div className="emp-card emp-empty">Đang tải hồ sơ...</div>;return <div><section className="emp-card" style={{textAlign:"center"}}><div className="emp-avatar" style={{margin:"0 auto 12px",background:"#1268d8"}}>{(profile?.fullName||"NV").split(/\s+/).slice(-2).map(x=>x[0]).join("").toUpperCase()}</div><h2>{profile?.fullName}</h2><p className="emp-muted">{profile?.employeeCode} · {profile?.position}</p></section><section className="emp-card emp-list"><div className="emp-list-item"><FiMail/><div><small>Email</small><b>{profile?.email||"Chưa cập nhật"}</b></div></div><div className="emp-list-item"><FiPhone/><div><small>Số điện thoại</small><b>{profile?.phone||"Chưa cập nhật"}</b></div></div><div className="emp-list-item"><FiMapPin/><div><small>Chi nhánh</small><b>{profile?.branchName||"Chưa phân công"}</b><small>{profile?.branchAddress}</small></div></div><div className="emp-list-item"><FiUser/><div><small>Ngày vào làm</small><b>{shortDate(profile?.hireDate)||"Chưa cập nhật"}</b></div></div></section><button className="emp-button danger" style={{width:"100%"}} onClick={logout}><FiLogOut/> Đăng xuất</button></div>}
