const r=require('express').Router(),auth=require('../middleware/authMiddleware'),roles=require('../middleware/roleMiddleware'),c=require('../controllers/chatController');r.use(auth,roles('employee','manager','admin'));r.get('/channels',c.channels);r.get('/channels/:id/messages',c.messages);r.post('/channels/:id/messages',c.send);r.put('/channels/:id/read',c.read);r.delete('/messages/:id',c.remove);module.exports=r;
