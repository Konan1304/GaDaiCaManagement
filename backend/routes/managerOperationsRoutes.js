const router=require("express").Router();const authenticate=require("../middleware/authMiddleware"),allowRoles=require("../middleware/roleMiddleware"),c=require("../controllers/managerOperationsController"),suite=require("../controllers/managerOperationSuiteController");
router.use(authenticate,allowRoles("manager","admin"));router.get("/mappings",c.mappings);router.put("/mappings",c.saveMapping);router.get("/assignments",c.assignments);router.post("/assignments",c.assign);router.get("/overview",c.overview);router.get("/clock",c.clock);router.put("/clock",c.updateClock);router.get("/dashboard",suite.dashboard);router.get("/timeline",suite.timeline);router.get("/reports/daily",suite.report);router.get("/reports/weekly",suite.report);router.get("/reports/monthly",suite.report);router.get("/export/excel",suite.excel);router.get("/export/pdf",suite.pdf);router.get("/shifts/:id/audit",suite.audit);router.get("/shifts/:id",suite.detail);router.post("/shifts/:id/lock",suite.lock);router.post("/shifts/:id/unlock",c.unlock);module.exports=router;
